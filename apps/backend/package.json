{
    "name": "backend",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": ".venv\\Scripts\\python -m uvicorn app.main:app --reload --reload-dir app --host 0.0.0.0 --port 8000"
    }
}